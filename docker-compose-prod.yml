version: '3'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile.prod
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - .:/code
      - media_volume:/code/media
      - static_volume:/code/staticfiles
    environment:
      - PYTHONUNBUFFERED=1

volumes:
  media_volume:
  static_volume: